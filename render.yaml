services:
  - type: web
    name: livora-api
    runtime: node
    plan: free
    buildCommand: npm install && npm run build && npx prisma generate
    # El proyecto no usa migraciones (no existe prisma/migrations), por eso
    # `migrate deploy` no aplicaba nada: db push sincroniza el esquema real.
    startCommand: npx prisma db push && node dist/main
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: DATABASE_URL
        sync: false
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_SERVICE_ROLE_KEY
        sync: false
      - key: SUPABASE_JWT_SECRET
        sync: false
      - key: WALLET_ENCRYPTION_KEY
        sync: false
      - key: REDIS_HOST
        sync: false
      - key: REDIS_PORT
        value: 6379
      - key: ARBITRUM_RPC_URL
        sync: false
      - key: WORKER_PRIVATE_KEY
        sync: false
      - key: ECOTOKEN_CONTRACT_ADDRESS
        sync: false
      - key: PINATA_API_KEY
        sync: false
      - key: PINATA_SECRET_KEY
        sync: false
      - key: IPFS_GATEWAY_URL
        sync: false
